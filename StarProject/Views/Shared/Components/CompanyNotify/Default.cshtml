@model List<StarProject.Models.CompanyNotify>

@for (int i = 0; i < Model.Count; i++)
{
    <div class="notify-item"
         style="display:@(i == 0 ? "block" : "none"); position:absolute; top:0; left:0; width:100%;">
        @Model[i].Title
    </div>
}

